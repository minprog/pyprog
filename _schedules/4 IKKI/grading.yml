# This is a separate section containing a few templates that we will use below.
# The & feature is built into YAML, it is not a special feature of the website.
# The section does not need to be called `templates` necessarily.

templates:
    1: &automatic
        type: pass
        subgrades:
            done: boolean
        automatic:
            done: -(correctness_score.floor)
        calculation: done
        auto_publish: true
        hide_calculated: true
    4: &autopoints
        type: float
        subgrades:
            points: integer
        automatic:
            points: correctness_score
        calculation: (points / 6.0 * 9 + 1).round(1)
        # auto_publish: true
        hide_subgrades: true
    2: &manual
        type: float
        subgrades:
            points: integer
        calculation: points
        # hide_subgrades: true
    3: &check
        type: pass
        subgrades:
            done: boolean
        calculation: done
        hide_calculated: true
    5: &werkcollege
        type: pass
        subgrades:
            aanwezig: boolean
            werkblad: boolean
        calculation: -((aanwezig+werkblad).abs/2)
        hide_calculated: true
    7: &werkcollege_her
        type: pass
        subgrades:
            helemaal_goed: boolean
        calculation: helemaal_goed
        hide_calculated: true
    6: &tentamen
        type: pass
        subgrades:
            voldoende: boolean
        automatic:
            voldoende: -(correctness_score.floor)
        calculation: voldoende
        hide_calculated: true


# GRADES - contains definitions of how grades are structured. The names of the
# grades match the names of submits that may be defined in `submit.yml`
# elsewhere, but you can also create grades without an accompanying submit.
#
# The following properties are supported:
#
# - `type` can be `integer`, `float` or `pass`, which influences presentation
# and input
#
# - `subgrades` defines any extra numbers to be added to a grade, with their
# types `integer`, `pass`, `boolean` or `float`
#
# - note: if any subgrades are defined, teaching assistants are only allowed to
# enter those, and can't override the grade itself
#
# - `calculation` is an expression that is evaluated in the context of the
# subgrades, which means you can use any subgrade by name in that expression, as
# well as any standard Ruby objects and syntax
#
# - `automatic` is an expression that is evaluated in the context of any
# automatic grading results (as defined in `submit.yml`) - allowing the use of
# the variable `correctness_score`. It can only be used to fill in subgrades,
# but you can then use the `calculation` to transfer the value of a subgrade
# into the grade.
#
# - `hide_calculated` hides the calculated grade, only showing the entered
# subgrades (to de-emphasize grades for students)
#
# - `hide_subgrades` hides the subgrades, for example if you want to do a simple
# calculation but only show the resulting grade to students
#
# You can leave this out if grading isn't used or if each submit simply requires
# entering a final grade, no subgrades and no calculations.

grades:

    tentamen_ik: *tentamen
    tentamen_ki: *tentamen
    hertentamen_ik: *tentamen
    hertentamen_ki: *tentamen

    werkcollege_1: *werkcollege
    werkcollege_2: *werkcollege
    werkcollege_3: *werkcollege
    werkcollege_4: *werkcollege
    werkcollege_5: *werkcollege
    werkcollege_6: *werkcollege
    werkcollege_7: *werkcollege
    tentamen_oefening_w2: *werkcollege

    oefententamen: *werkcollege
    oefenen_hertentamen: *werkcollege
    
    werkcollege_1_her: *werkcollege_her
    werkcollege_2_her: *werkcollege_her
    werkcollege_3_her: *werkcollege_her
    werkcollege_4_her: *werkcollege_her
    werkcollege_5_her: *werkcollege_her
    werkcollege_6_her: *werkcollege_her

    modules:
        type: points
        subgrades:
            mod_5: boolean
            virus: boolean
            mod_6: boolean
            klima: boolean
            mod_7: boolean
            adven: boolean
        calculation: -(mod_5 * 5 + virus * 3 + mod_6 * 5 + klima * 3 + mod_7 * 5 + adven * 3)

# CALCULATIONS - define the final grades for the course and the weights between
# subcomponents for each. Often, multiple final grades are differentiated by the
# choice of grades that are used for the calculation, for example calculating
# the "normal" final grade based on the "first exam", and the "resit" final
# grade based on the result of the second exam. Currently, running "calculate
# final grades" on the website will try to calculate all, which might result in
# useless non-grades getting calculated.
#
# Weights specify proportionally to each other and do not need to add up to
# anything in particular. If a weight of 0 is specified, the grade is not used
# in the calculation, but the grade must be "passed" for the calculation to
# succeed.
#
# You can leave this out if the site does not need to perform final grade
# calculations.

calculation:
    eindcijfer_ki:
        werkcolleges: 7
        modules: 24
        werkbladen: 7
        tentamen_ki: 14
    eindcijfer_ik:
        werkcolleges: 7
        modules: 24
        werkbladen: 7
        tentamen_ik: 14
    herkansing_ki:
        werkcolleges: 7
        modules: 24
        werkbladen: 7
        hertentamen_ki: 14
    herkansing_ik:
        werkcolleges: 7
        modules: 24
        werkbladen: 7
        hertentamen_ik: 14

# SUB-CALCULATIONS - define the components of the course that may be used in
# final grade calculations. A few constraints can be specified.
#
# The following properties are supported:
#
# `submits` are the names of the grades/submits with accompanying weight for
# each
#
# `show_progress: true` will list the results of the grades in a table on
# students' progress pages
#
# `required: true` will only "pass" this part if all comprising grades are
# passed (used with pass/fail tests)
#
# `minimum: 5.5` will only "pass" this part if the weighed average of the grades
# is at least the specified minimum
#
# You can leave these out if unused in the `calculation` section.

werkcolleges:
    show_progress: true
    submits:
        werkcollege_1: 1
        werkcollege_2: 1
        werkcollege_3: 1
        werkcollege_4: 1
        werkcollege_5: 1
        werkcollege_6: 1
        werkcollege_7: 1

werkbladen:
    show_progress: true
    type: points
    submits:
        werkcollege_1_her: 1
        werkcollege_2_her: 1
        werkcollege_3_her: 1
        werkcollege_4_her: 1
        werkcollege_5_her: 1
        werkcollege_6_her: 1
        werkcollege_7: 1

module_1:
    show_progress: true
    deadline: 3/11/24 17:00
    submits:
        weken: 1
        water: 1
        binair: 1
        cafeine: 1
        vakantie: 1
        acid: 1
        orakel: 1
        byte: 1
        rechthoeken: 1
        priem_getal: 1

module_2:
    show_progress: true
    deadline: 10/11/24 17:00
    submits:
        cola: 1
        greedy: 1
        raadspel: 1
        populatie: 1
        piramide: 1
        temperatuur: 1
        kalender: 1
        monopoly: 1

module_3:
    # show_overview: true
    show_progress: true
    deadline: 17/11/24 17:00
    submits:
        camelcase: 1
        rna: 1
        alfabet: 1
        etenstijd: 1
        calculator: 1
        wachtwoord: 1
        leesbaarheid: 1
        pwdgen: 1

module_5:
    # show_overview: true
    show_progress: true
    deadline: 1/12/24 17:00
    deadline_hard: true
    submits:
        scrabble: 1
        selection_sort: 1
        special_sort: 1
        sjoelen: 1
        schuifpuzzel: 1

module_5_project:
    # show_overview: true
    show_progress: true
    deadline: 1/12/24 17:00
    deadline_hard: true
    submits:
        virus: 1

module_6:
    # show_overview: true
    show_progress: true
    deadline: 8/12/24 17:00
    deadline_hard: true
    submits:
        indexer: 1
        eca2csv: 1

module_6_project:
    # show_overview: true
    show_progress: true
    deadline: 8/12/24 17:00
    deadline_hard: true
    submits:
        klimaat: 1

module_7:
    # show_overview: true
    show_progress: true
    deadline: 15/12/24 17:00
    deadline_hard: true
    submits:
        # queue: 1
        cards: 1
        country: 1
        lines: 1
        lisp: 1
        figuren: 1

module_7_project:
    # show_overview: true
    show_progress: true
    deadline: 15/12/24 17:00
    deadline_hard: true
    submits:
        adventure-more: 1

modules:
    show_overview: true
    type: points
    submits:
        modules: 24

tentamen_ki:
    show_overview: true
    type: points
    submits:
        tentamen_ki: 14

tentamen_ik:
    show_overview: true
    type: points
    submits:
        tentamen_ki: 14

hertentamen_ki:
    # show_overview: true
    type: points
    submits:
        tentamen_ki: 14

hertentamen_ik:
    # show_overview: true
    type: points
    submits:
        tentamen_ki: 14
