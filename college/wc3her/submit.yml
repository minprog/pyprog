name: werkcollege_3_her
exam: true
buttons:
    Run doctest: |
        import <filename>
        import doctest
        doctest.testmod(<filename>, verbose=True)
files:
    required:
        formules.py: |
            # Schrijf hieronder al je string-functies.
