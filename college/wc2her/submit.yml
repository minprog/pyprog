name: werkcollege_2_her
exam: true
buttons:
    Run doctest: |
        import <filename>
        import doctest
        doctest.testmod(<filename>, verbose=True)
files:
    required:
        formules.py: |
            # Schrijf hieronder al je loops.
check:
    tool: checkpy
    repo: minprog/pytent
    args: -m wc2
autocheck: false
