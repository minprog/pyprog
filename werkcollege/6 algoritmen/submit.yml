name: werkcollege_6
exam: true
buttons:
    Run doctest: |
        import <filename>
        import doctest
        doctest.testmod(<filename>, verbose=True)
    Run typecheck: |
        from mypy import api as mypy_api
        mypy_api.run(['--strict', '<filename>.py'])[0]
files:
    required:
        lists.py: |
            # Schrijf hieronder al je functies.
            # Elke functie moet minstens 2 doctests hebben.
            #
            # Let op: gebruik de knop "Run doctest" om je code uit te
            # proberen en "Run typecheck" om de types te checken.
